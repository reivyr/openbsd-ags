$OpenBSD$

Index: Engine/Makefile
--- Engine/Makefile.orig
+++ Engine/Makefile
@@ -18,7 +18,7 @@ ifneq ($(USE_MIDI_PATCH), 1)
 AL_MIDI_PATCH :=
 endif
 
-OBJS := $(ALMP3) $(ALOGG) $(APEG) $(AASTR) $(HQ2X3X) $(AL_MIDI_PATCH) $(CDA) $(ALDUMB) $(GLAD) $(PLUGINS) $(BASE) $(BASE_PLATFORM)
+OBJS := $(ALMP3) $(ALOGG) $(APEG) $(AASTR) $(HQ2X3X) $(GLAD) $(PLUGINS) $(BASE) $(BASE_PLATFORM)
 OBJS := $(OBJS:.mm=.o)
 OBJS := $(OBJS:.cpp=.o)
 OBJS := $(OBJS:.c=.o)
